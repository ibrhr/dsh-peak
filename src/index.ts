/**
 * dsh-peak · DeepSeek API Peak & Off-Peak status indicator plugin for DeepSeek Harness (DSH).
 *
 * Node entry point.
 */

import type { Context } from 'cordis'

export const name = '@dsh-external/dsh-peak'

/**
 * Register plugin with Cordis host context.
 */
export function apply(_ctx: Context): void {
  // Visual presentation plugin for DSH Web UI.
}

export default apply
